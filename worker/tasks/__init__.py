# indexing.py, aggregates.py, maintenance.py land in Phase 5 — architecture
# plan §5. Empty in Phase 0 so the Celery app has a real (if task-less)
# autodiscover target and the Docker/CI build matrix has something to build.
