# Django
db.sqlite3
*.log

# Python
__pycache__/
*.py[cod]
*.pyo

#Virtual environment
venv/

# Environment variables / secrets
.env

# Django static/media
staticfiles/
media/

# Redis (local worker dev — dropped into the cwd when redis-server runs
# without an explicit --dir)
dump.rdb