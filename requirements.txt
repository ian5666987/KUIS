asgiref==3.11.1
Django==6.0.4
django-environ==0.11.2
psycopg2-binary==2.9.12
sqlparse==0.5.5
tzdata==2026.1

# --- FastAPI data plane (dataplane/) ---------------------------------------
# Architecture plan §1/§8 Phase 0: health-check only for now. SQLAlchemy/
# asyncpg join this list in Phase 2 when dataplane/models/tables.py and the
# first real repository land.
fastapi==0.121.2
uvicorn[standard]==0.38.0
pydantic-settings==2.12.0

# --- Worker (worker/) -------------------------------------------------------
# Architecture plan §5/§8 Phase 0: Celery app boots with zero tasks
# registered. Real tasks (index_document, compute_document_word_freq, ...)
# land in Phase 5.
celery==5.6.0
redis==7.1.0
